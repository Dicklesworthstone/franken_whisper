# Ambition bar

The pass is full-scope and exceeds the ten-change bar. It changes discovery, model resolution, robot errors, installer admission and authentication, two-binary packaging, pinned sibling reproduction, DSR registration, dormant workflow authority, documentation, and regression coverage. It does not weaken diarization certification, conformance, privacy, or performance gates, and it does not publish or download Sortformer weights.

The implementation bar is met with sixteen applied changes across all five critical surfaces. Two fresh-eyes rounds were completed: the first covered Rust CLI/runtime/model discovery, and the second covered installer/release/documentation/privacy failure paths. Gate results remain an independent completion condition and are recorded in the handoff rather than converted into ergonomics credit.
