# xcodegen spec for the FrankenWhisper iOS app. Regenerate with: (cd ios && xcodegen generate)
name: FrankenWhisper
options:
  bundleIdPrefix: com.frankenwhisper
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.10"
    # The Rust simulator library is arm64-only (Apple Silicon hosts). Building the
    # x86_64 sim slice would need the x86_64-apple-ios Rust target added to
    # ios/build-rust.sh; until someone needs an Intel Mac, exclude it.
    "EXCLUDED_ARCHS[sdk=iphonesimulator*]": x86_64
    MARKETING_VERSION: "1.1"
    CURRENT_PROJECT_VERSION: "3"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: AU8V2Z6NKY
    ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
targets:
  FrankenWhisper:
    type: application
    platform: iOS
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.frankenwhisper.FrankenWhisper
        TARGETED_DEVICE_FAMILY: "1,2"
        INFOPLIST_FILE: Sources/Info.plist
        CODE_SIGN_ENTITLEMENTS: FrankenWhisper.entitlements
        FRAMEWORK_SEARCH_PATHS: ["$(inherited)", "."]
        SUPPORTS_MACCATALYST: true
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: false
        DERIVE_MACCATALYST_PRODUCT_BUNDLE_IDENTIFIER: false
        MACCATALYST_USE_NATIVE_DESIGN: true
      configs:
        Release:
          SWIFT_COMPILATION_MODE: wholemodule
          SWIFT_OPTIMIZATION_LEVEL: "-O"
          ENABLE_NS_ASSERTIONS: false
    sources:
      - Sources
      - Shared
      - Assets.xcassets
    dependencies:
      - framework: FwCore.xcframework
        embed: false
      - sdk: Accelerate.framework
      - target: FrankenWhisperKeyboard
        embed: true
        platformFilter: iOS
      - target: FrankenWhisperWidgets
        embed: true
        platformFilter: iOS
      - target: FrankenWhisperShare
        embed: true
        platformFilter: iOS
    entitlements:
      path: FrankenWhisper.entitlements
      properties:
        # 8 GB devices grant ~4-5 GB with this instead of the ~3.3 GB default. The
        # working set is ~0.9 GB of q8_0 whisper weights + ~0.5 GB Sortformer +
        # decode scratch, plus transient load buffers; the headroom keeps jetsam
        # away during model load.
        com.apple.developer.kernel.increased-memory-limit: true
        com.apple.security.application-groups:
          - group.com.frankenwhisper.dictation
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: FrankenWhisper
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        CFBundleURLTypes:
          - CFBundleTypeRole: Editor
            CFBundleURLName: com.frankenwhisper.FrankenWhisper.dictation
            CFBundleURLSchemes:
              - frankenwhisper
        NSUserActivityTypes:
          - com.frankenwhisper.workspace
        ITSAppUsesNonExemptEncryption: false
        NSSupportsLiveActivities: true
        UILaunchScreen: {}
        NSMicrophoneUsageDescription: >-
          Recordings are transcribed entirely on this device by a local model.
          No audio, transcript, or anything else ever leaves your phone.
        NSPhotoLibraryAddUsageDescription: >-
          Save a captioned video you created locally with FrankenWhisper back to Photos.
        UIBackgroundModes:
          - audio
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight

  FrankenWhisperWidgets:
    type: app-extension
    platform: iOS
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.frankenwhisper.FrankenWhisper.Widgets
        PRODUCT_NAME: FrankenWhisper Widgets
        APPLICATION_EXTENSION_API_ONLY: true
        SKIP_INSTALL: true
        SUPPORTS_MACCATALYST: false
    sources:
      - Widget
      - Shared/FrankenWhisperSystemState.swift
      - path: Sources/PrivacyInfo.xcprivacy
    entitlements:
      path: FrankenWhisperWidgets.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.frankenwhisper.dictation
    info:
      path: Widget/Info.plist
      properties:
        CFBundleDisplayName: FrankenWhisper Widgets
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension

  FrankenWhisperShare:
    type: app-extension
    platform: iOS
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.frankenwhisper.FrankenWhisper.Share
        PRODUCT_NAME: Transcribe with FrankenWhisper
        APPLICATION_EXTENSION_API_ONLY: true
        SKIP_INSTALL: true
        SUPPORTS_MACCATALYST: false
    sources:
      - Share
      - Shared/FrankenWhisperSystemState.swift
      - path: Sources/PrivacyInfo.xcprivacy
    entitlements:
      path: FrankenWhisperShare.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.frankenwhisper.dictation
    info:
      path: Share/Info.plist
      properties:
        CFBundleDisplayName: Transcribe with FrankenWhisper
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.share-services
          NSExtensionPrincipalClass: "$(PRODUCT_MODULE_NAME).ShareViewController"
          NSExtensionAttributes:
            NSExtensionActivationRule:
              NSExtensionActivationSupportsFileWithMaxCount: 1
              NSExtensionActivationSupportsMovieWithMaxCount: 1

  FrankenWhisperKeyboard:
    type: app-extension
    platform: iOS
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.frankenwhisper.FrankenWhisper.Keyboard
        PRODUCT_NAME: FrankenWhisper Keyboard
        TARGETED_DEVICE_FAMILY: "1,2"
        INFOPLIST_FILE: Keyboard/Info.plist
        CODE_SIGN_ENTITLEMENTS: FrankenWhisperKeyboard.entitlements
        APPLICATION_EXTENSION_API_ONLY: true
        SKIP_INSTALL: true
    sources:
      - Keyboard
      - Shared/DictationBridge.swift
    entitlements:
      path: FrankenWhisperKeyboard.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.frankenwhisper.dictation

  FrankenWhisperTests:
    type: bundle.unit-test
    platform: iOS
    settings:
      base:
        GENERATE_INFOPLIST_FILE: true
    sources:
      - Tests
      - Sources/LiveUtteranceDetector.swift
      - Sources/LiveSpectrum.swift
      - Sources/SubtitleTimeline.swift
      - Sources/SubtitleVideoExporter.swift
